public class Joint {
  public Vec2 position;
  public float angle;
  public float length;

  public Joint(Vec2 position, float angle, float length) {
    this.position = position;
    this.angle = angle;
    this.length = length;
  }
}
